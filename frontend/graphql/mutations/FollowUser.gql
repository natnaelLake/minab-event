mutation InsertFollow($followerId: uuid!, $userId: uuid!) {
  insert_follows_one(
    object: { created_at: "now()", follower_id: $followerId, user_id: $userId }
  ) {
    created_at
    follower_id
    user {
      first_name
      id
    }
    id
  }
}
