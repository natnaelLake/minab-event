mutation InsertFollow($followerId: uuid!) {
  insert_follows_one(
    object: {
      created_at: "now()"
      follower_id: $followerId
    }
  ) {
    created_at
    follower_id
    user {
      first_name
      id
    }
    id
  }
}
