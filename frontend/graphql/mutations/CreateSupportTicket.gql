mutation CreateSupportTicket($title: String!, $description: String!) {
  insert_support_tickets_one(
    object: { title: $title, description: $description }
  ) {
    id
    title
    description
  }
}
