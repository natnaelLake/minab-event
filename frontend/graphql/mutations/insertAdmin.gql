mutation CreateAdmin(
  $email: String!
  $first_name: String!
  $last_name: String!
  $role: String!
) {
  insert_users(
    objects: {
      last_name: $last_name
      email: $email
      role: $role
      first_name: $first_name
      password:"123456"
    }
  ) {
    affected_rows
  }
}
